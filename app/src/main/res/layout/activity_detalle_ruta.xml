<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.bitacoraschool.usuario.bitacora.DetalleRuta">


    
    <ListView
        android:id="@+id/ruta_detalle_listview"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    </ListView>

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/llegofloatbtn"
            android:src="@mipmap/ic_done_all_black_24dp"
            android:tint="@color/White_Snow"
            android:layout_gravity="right|bottom"
            android:layout_margin="26dp"
            android:onClick="Llego"
            app:fabSize="normal"
            android:visibility="gone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

    <ProgressBar
        android:id="@+id/detalleRutaPG"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_gravity="center"
        android:visibility="visible"/>
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/reloadButtonDetalleRuta"
        android:src="@mipmap/ic_autorenew_white_24dp"
        android:tint="@color/White_Snow"
        android:layout_gravity="center|center"
        android:layout_margin="26dp"
        android:onClick="reload"
        app:fabSize="normal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"/>

</android.support.design.widget.CoordinatorLayout>
